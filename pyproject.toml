[project]
name = "ansible-control"
version = "0"
description = "Ansible control-node tooling for this repo"
requires-python = ">=3.11"
dependencies = [
    "ansible-core>=2.16",
    "ansible-lint>=24.0",
    "yamllint>=1.35",
]
