---
- name: Allow passwordless shutdown for sudo group
  ansible.builtin.template:
    src: shutdown.j2
    dest: /etc/sudoers.d/shutdown
    owner: root
    group: root
    mode: "0440"
    validate: visudo -cf %s
  become: true
